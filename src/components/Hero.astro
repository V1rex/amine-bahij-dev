<section class="hero">
  <div class="container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow">Backend systems · Operations Research · Applied optimization</p>

      <h1>Amine Bahij</h1>

      <p class="subtitle">
        Backend systems, operations research, and optimization-driven software.
      </p>

      <p class="intro">
        I build maintainable backend systems and explore how mathematical optimization
        can be integrated into usable software for logistics, energy, and operational decision problems.
      </p>

      <div class="actions">
        <a class="button primary" href="/projects">View Projects</a>
        <a class="button secondary" href="/writing">Read Writing</a>
        <a class="button ghost" href="/about/">About Me</a>
      </div>
    </div>

    <div class="visual card" aria-label="Optimization visual">
      <div class="formula">
        <span class="small">minimize</span>
        <strong>cᵀx</strong>
        <span class="small">subject to</span>
        <code>Ax ≤ b</code>
        <code>x ≥ 0</code>
      </div>

      <svg viewBox="0 0 360 260" role="img" aria-label="Feasible region diagram">
        <defs>
          <pattern id="grid" width="20" height="20" patternUnits="userSpaceOnUse">
            <path d="M 20 0 L 0 0 0 20" fill="none" stroke="#e2e8f0" stroke-width="1" />
          </pattern>
        </defs>

        <rect width="360" height="260" rx="18" fill="url(#grid)" />

        <!-- axes -->
        <line x1="56" y1="210" x2="310" y2="210" stroke="#64748b" stroke-width="2" />
        <line x1="56" y1="210" x2="56" y2="42" stroke="#64748b" stroke-width="2" />

        <!-- feasible region -->
        <polygon
            points="82,190 138,116 220,82 282,132 238,190"
            fill="#dbeafe"
            fill-opacity="0.72"
            stroke="#2563eb"
            stroke-width="2.3"
        />

        <!-- normal vertices -->
        <circle cx="82" cy="190" r="4.5" fill="#2563eb" />
        <circle cx="138" cy="116" r="5.5" fill="#2563eb" />
        <circle cx="220" cy="82" r="5.8" fill="#1d4ed8" />
        <circle cx="238" cy="190" r="4.5" fill="#2563eb" />

        <!-- highlighted optimal vertex -->
        <circle cx="282" cy="132" r="10" fill="#93c5fd" opacity="0.95" />
        <circle cx="282" cy="132" r="5.5" fill="#2563eb" />

        <!-- axis labels -->
        <text x="24" y="48" fill="#64748b" font-size="14">x₂</text>
        <text x="298" y="232" fill="#64748b" font-size="14">x₁</text>
      </svg>

      <div class="api-tag" aria-label="Optimization API example">
        <code>
          <span class="method">POST</span>
          <span class="endpoint">/optimize</span>
          <span class="arrow">→</span>
          <span class="result">optimal_decision</span>
        </code>
      </div>



    </div>
  </div>
</section>

<style>
  .hero {
    padding: 6.5rem 0 4.5rem;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(320px, 0.75fr);
    gap: 3.5rem;
    align-items: center;
  }

  .eyebrow {
    margin: 0 0 1.25rem;
    color: var(--accent);
    font-size: 0.82rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0;
    font-size: clamp(3.5rem, 8vw, 6.6rem);
    line-height: 0.92;
    letter-spacing: -0.075em;
  }

  .subtitle {
    max-width: 680px;
    margin: 1.5rem 0 0;
    color: var(--text-muted);
    font-size: clamp(1.35rem, 3vw, 1.9rem);
    line-height: 1.35;
    letter-spacing: -0.03em;
  }

  .intro {
    max-width: 640px;
    margin: 1.4rem 0 0;
    color: var(--text-muted);
    font-size: 1.05rem;
    line-height: 1.85;
  }

  .actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 2.1rem;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-md);
    padding: 0.85rem 1.15rem;
    font-weight: 800;
    transition:
        color 240ms ease,
        background-color 240ms ease,
        border-color 240ms ease,
        transform 240ms ease,
        box-shadow 240ms ease;
  }

  .button.ghost {
    border: 1px solid transparent;
    background: transparent;
    color: var(--text-muted);
  }

  .button.ghost:hover {
    color: var(--accent);
    background: rgba(37, 99, 235, 0.06);
    transform: translateY(-1px);
  }

  .primary {
    background: var(--accent);
    color: white;
  }

  .primary:hover {
    background: var(--accent-dark);
  }

  .secondary {
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--text-main);
  }

  .visual {
    padding: 1rem;
  }

  .formula {
    display: grid;
    gap: 0.35rem;
    margin-bottom: 0.8rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    background: var(--surface-muted);
    padding: 1rem;
  }

  .formula strong {
    font-size: 1.7rem;
    letter-spacing: -0.04em;
  }

  .formula code {
    color: var(--text-muted);
    font-size: 1rem;
  }

  .small {
    color: var(--text-soft);
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  svg {
    width: 100%;
    height: auto;
    display: block;
  }

  .api-tag {
    margin-top: 0.9rem;
    border: 1px solid rgba(15, 23, 42, 0.9);
    border-radius: var(--radius-md);
    background: #020617;
    padding: 0.85rem 1rem;
    box-shadow: 0 18px 35px rgba(15, 23, 42, 0.16);
  }

  .api-tag code {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    flex-wrap: wrap;
    color: #e5e7eb;
    font-size: 0.92rem;
    font-weight: 700;
    letter-spacing: -0.01em;
  }

  .method {
    color: #93c5fd;
  }

  .endpoint {
    color: #f8fafc;
  }

  .arrow {
    color: #64748b;
  }

  .result {
    color: #60a5fa;
  }

  @media (max-width: 900px) {
    .hero-grid {
      grid-template-columns: 1fr;
    }

    .visual {
      max-width: 520px;
    }
  }
</style>